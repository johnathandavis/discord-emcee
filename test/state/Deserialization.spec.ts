import type { InteractionDeserializer, InteractionDeserializerMap } from '../../src/state/Common';

describe('Deserialization', () => {
    test('optionDeserializer deserializes string values', () => {
        
    });
});